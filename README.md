# Homebrew Tap for Godot (No UID)

Install the custom "No UIDs" build of Godot easily via Homebrew.

## Installation

```bash
brew tap rafaismyname/godot-no-uids
brew install --cask godot-no-uids
```

## Updates

When a new version is released, simply run:

```bash
brew upgrade godot-no-uids
```
